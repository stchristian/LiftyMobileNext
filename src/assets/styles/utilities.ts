import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  row: {
    flexDirection: 'row',
  },
  row_center: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  fluid: {
    marginHorizontal: -16,
  },
});
