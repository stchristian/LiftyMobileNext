<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:headerBackground">@color/primary</item>
        <item name="android:textColorHint">@color/grey</item>
        <item name="android:textColorHighlight">@color/grey</item>
        <item name="android:textColorHintInverse">@color/grey</item>
        <item name="android:textColorLink">@color/grey</item>
        <item name="android:textColorAlertDialogListItem">@color/grey</item>
        <item name="android:textColorSecondary">@color/grey</item>
        <item name="android:timePickerStyle">@style/Widget.Material.Light.TimePicker</item>
    </style>

</resources>
